{
  "name": "nexaed-sdk",
  "private": true,
  "packageManager": "pnpm@10.11.1",
  "scripts": {
    "build": "pnpm -r --filter '!@nexa-ed/config' build",
    "check-types": "pnpm -r --filter '!@nexa-ed/config' check-types",
    "version-packages": "changeset version",
    "release": "changeset publish",
    "changeset": "changeset"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0"
  }
}
