/**
 * @nexa-ed/react
 *
 * Pre-built React components and hooks for the Nexa Education Platform.
 *
 * Components are migrated from packages/ocr-ui and re-exported here
 * for tenant app consumption. This package is intentionally scaffolded
 * — components are added here as each feature stabilises in ocr-ui.
 *
 * Planned exports:
 *   DocumentViewer        — full pipeline timeline + status
 *   StudentRecordsGrid    — editable data grid with sorting/pagination
 *   PageDetailPanel       — per-page record review and editing
 *   ProgressTracker       — live SSE progress bar (uses /api/nexa/progress)
 *   RefinementButton      — trigger LLM re-refinement for a page
 *   UploadZone            — drag-and-drop upload wired to nexa.files.submit()
 *   StatusBanner          — processing status banner
 */

// Components will be added here as they graduate from packages/ocr-ui
// export { DocumentViewer } from "./components/DocumentViewer";
// export { StudentRecordsGrid } from "./components/StudentRecordsGrid";
// export { ProgressTracker } from "./components/ProgressTracker";

export {};
